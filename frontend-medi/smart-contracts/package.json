{
    "name": "medichain-labs-smart-contracts",
    "version": "0.0.1",
    "description": "Smart contracts for Medichain Labs on the Aptos blockchain",
    "main": "index.js",
    "scripts": {
      "build": "aptos move compile --package-dir ./",
      "test": "npx mocha 'tests/**/*.test.ts'",
      "deploy": "aptos move publish --package-dir ./ --named-addresses MedichainLabs=0xYourModuleAddress",
      "clean": "rm -rf build"
    },
    "author": "Your Name <your.email@example.com>",
    "license": "Apache-2.0",
    "dependencies": {
      "aptos": "^1.0.0",  
      "mocha": "^10.0.0",  
      "chai": "^4.0.0",    
      "typescript": "^4.0.0", 
      "@types/mocha": "^10.0.0", 
      "@types/chai": "^4.0.0"   
    },
    "devDependencies": {
      "ts-node": "^10.0.0" 
    }
  }
  